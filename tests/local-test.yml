---
- hosts: localhost
  gather_facts: no
  connection: local

 # note: this assumes these domains are created!
  vars:
    virtvars_node_list:
      - tiny0
      - tiny2

  roles:
  - ansible-role-virtvars


